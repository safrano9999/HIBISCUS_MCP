# Optional gateway bearer. Skip to use direct bearer passthrough.
#secret
HIBISCUS_MCP_GATEWAY=example: openssl rand -hex 32

# Real Hibiscus credential used only in gateway mode.
#secret
HIBISCUS_MCP_BEARER=
