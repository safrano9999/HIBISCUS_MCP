diff --git a/src/gateways/stdioToStatefulStreamableHttp.ts b/src/gateways/stdioToStatefulStreamableHttp.ts
index c912900..ac29e3a 100644
--- a/src/gateways/stdioToStatefulStreamableHttp.ts
+++ b/src/gateways/stdioToStatefulStreamableHttp.ts
@@ -14,6 +14,7 @@ import { SessionAccessCounter } from '../lib/sessionAccessCounter.js'
 
 export interface StdioToStreamableHttpArgs {
   stdioCmd: string
+  host: string
   port: number
   streamableHttpPath: string
   logger: Logger
@@ -39,6 +40,7 @@ export async function stdioToStatefulStreamableHttp(
 ) {
   const {
     stdioCmd,
+    host,
     port,
     streamableHttpPath,
     logger,
@@ -51,7 +53,7 @@ export async function stdioToStatefulStreamableHttp(
   logger.info(
     `  - Headers: ${Object(headers).length ? JSON.stringify(headers) : '(none)'}`,
   )
-  logger.info(`  - port: ${port}`)
+  logger.info(`  - listen: ${host}:${port}`)
   logger.info(`  - stdio: ${stdioCmd}`)
   logger.info(`  - streamableHttpPath: ${streamableHttpPath}`)
 
@@ -299,8 +301,8 @@ export async function stdioToStatefulStreamableHttp(
   // Handle DELETE requests for session termination
   app.delete(streamableHttpPath, handleSessionRequest)
 
-  app.listen(port, () => {
-    logger.info(`Listening on port ${port}`)
+  app.listen(port, host, () => {
+    logger.info(`Listening on ${host}:${port}`)
     logger.info(
       `StreamableHttp endpoint: http://localhost:${port}${streamableHttpPath}`,
     )
diff --git a/src/index.ts b/src/index.ts
index e456853..0a8b376 100644
--- a/src/index.ts
+++ b/src/index.ts
@@ -65,6 +65,11 @@ async function main() {
       default: 8000,
       description: '(stdio→SSE, stdio→WS) Port for output MCP server',
     })
+    .option('host', {
+      type: 'string',
+      default: '0.0.0.0',
+      description: '(stdio→HTTP) Address for the output MCP server to bind',
+    })
     .option('baseUrl', {
       type: 'string',
       default: '',
@@ -210,6 +215,7 @@ async function main() {
 
           await stdioToStatefulStreamableHttp({
             stdioCmd: argv.stdio!,
+            host: argv.host,
             port: argv.port,
             streamableHttpPath: argv.streamableHttpPath,
             logger,
